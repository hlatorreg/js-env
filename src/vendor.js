/**
 * Este archivo contiene referencias a todas las librerias de terceros que vamos a usar,
 * en este caso solamente whatwg-fetch, en una aplicacion real se referenciaria Angular,
 * react, jQuery, bootrap, etc...
 * El objetivo es que el usuario solamente descargue las librerias si existen cambios en ellas,
 * cosa que no es tan frecuente.
 */

 /* eslint-disable no-unused-vars */

 import fetch from 'whatwg-fetch';